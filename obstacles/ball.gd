extends StaticBody2D

signal bounce

#func _process(delta):
#	move_and_slide(delta)
#	if get_slide_count() > 0:
#		emit_signal("bounce")


#if get_slide_count() > 0:
#      var collision = get_slide_collision(0)
